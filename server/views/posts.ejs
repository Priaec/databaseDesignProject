<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Posts</title>
  </head>
  <body>
    <a href="search?userName=<%= userName %>">Back</a>
    <div>
    <p>Items you mentioned 'Excellent' or 'Good'</p>
        <% highRatings.forEach(item => { %>
            <p class="review"><%= item.title %></p>
        <% }) %>
    </div>
    <div>
        <p>People Who have not written a poor review</p>
        <% poorResults.forEach(item => { %>
            <p class="review"><%= item.username %></p>
        <% }) %>
    </div>
  </body>
  <style>
    div{
        background-color: cadetblue;
        width: fit-content;
    }
  </style>
</html>